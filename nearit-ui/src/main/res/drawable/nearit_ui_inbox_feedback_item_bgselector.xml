<?xml version="1.0" encoding="utf-8"?>

<selector xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!--
    We make the pressed and focused selector items transparent so the ListView's own selector states show through.
    -->
    <!--<item android:state_pressed="true"
        android:drawable="@android:color/transparent"
        />
    <item android:state_focused="true"
        android:drawable="@android:color/transparent"
        />-->
    <item android:drawable="@drawable/nearit_ui_inbox_unread_background" app:state_message_unread="true" />
    <item android:drawable="@drawable/nearit_ui_inbox_read_background" />
</selector>
